# TASK SM-K4-04: Unit-test `path_verification` blocking semantics for K4 return

## Recipe step
2 graph edge (test only — no production verification promotion)

## Model
Luna

## Context (minimal)
- Today `path_verification(Varia→Speed)` blocking edge is `varia_to_kraid`
  because verification is `controller_dev` (not continuous)
- SM-K4-01 already locks edge ids + verification matrix on the live graph
- After pure reverse hops land, blocking should advance one edge at a time when
  verification is raised to `controller_dev` — encode that in a **fixture**,
  not by editing production edges

## Read first (only these)
- `super_metroid/tests/test_progression.py`
  (`test_k4_graph_varia_to_speed_scaffold`,
   `test_k4_graph_locks_varia_return_edge_contract`)
- `super_metroid/progression.py` — `path_verification`, `DoorEdge`,
  `RoomProgressionGraph` construction helpers

## Do
1. Keep live-graph asserts: blocking on START_TO_SPEED_GRAPH Varia→Speed is
   still `varia_to_kraid` (already covered — do not weaken).
2. Add a focused unit test that builds a **local** mini graph (or deep-copies
   edges inside the test) demonstrating:
   - When only `varia_to_kraid` is `controller_dev` and the next reverse edge is
     `unverified`, path is not all_continuous and blocking is the first
     non-continuous edge (`varia_to_kraid` today, or whatever the API returns
     for the first edge that is not `continuous` — match actual API semantics
     and document them in the test name/docstring).
   - When the test raises `varia_to_kraid` to `continuous` **only on the local
     fixture**, blocking advances to `kraid_to_eye_return` while that edge is
     still `unverified` / `controller_dev`.
3. Prefer constructing a tiny `RoomProgressionGraph` with 2–3 DoorEdges over
   mutating module globals.

## Do not
- Change live `_K4_SPEED_EDGES` verification levels
- Promote production edges to continuous
- Touch STATUS / continuous.py / controllers

## Acceptance
- [ ] `uv run pytest super_metroid/tests/test_progression.py -q` green
- [ ] New test encodes local-fixture blocking advance for K4 return
- [ ] No production graph mutation
- [ ] Diff summary in final message

## Verify commands
```bash
uv run pytest super_metroid/tests/test_progression.py -q
```
