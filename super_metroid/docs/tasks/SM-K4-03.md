# TASK SM-K4-03: Tracker rows for K4 reverse hops (CSV only)

## Recipe step
docs / tracker (no continuous claim)

## Model
Flash

## Context (minimal)
- Tracker: `docs/routes/KPDR_TRACKER.csv`
- Export: `scripts/export/kpdr_tracker.py` regenerates md/json
- Known: K3.2 `varia_to_kraid` is controller_dev; K3.3 is a lumped open row
- Graph edge ids (locked by SM-K4-01): `varia_to_kraid`, `kraid_to_eye_return`,
  `eye_to_baby_return`, `baby_to_kihunter_return`, `kihunter_to_zeela_return`,
  `zeela_to_warehouse_return`, then continuous `warehouse_to_business`
- Scaffold exists for `kraid_to_eye_return` only (still pure-unverified)

## Read first (only these)
- `super_metroid/docs/routes/KPDR_TRACKER.csv` (rows K3.2–K4.0)
- `super_metroid/scripts/export/kpdr_tracker.py`
- `super_metroid/progression.py` reverse edge ids under `_K4_SPEED_EDGES`

## Do
1. Split K3.3 lumped row into chartable reverse-hop rows. Suggested order ids:
   - Keep **K3.2** controller_dev — Varia → Kraid (`varia_to_kraid`)
   - **K3.3** open — Kraid → Eye return (`kraid_to_eye_return`, 0xA59F→0xA56B)
   - **K3.4** open — Eye → Baby return
   - **K3.5** open — Baby → Kihunter return
   - **K3.6** open — Kihunter → Zeela return
   - **K3.7** open — Zeela → Warehouse return
   - **K3.8** continuous (or note) — Warehouse → Business (reuse existing continuous edge)
   - Bump Speed row order if needed so K4.0 still follows reverse spine
2. Fill CSV columns consistently with neighbors (room hex, predecessor seg_id,
   probe_command where known: `kpdr.py pure kraid-to-eye-return` for K3.3)
3. Run export to refresh `KPDR_TRACKER.md` + json
4. Status honesty: only K3.2 is controller_dev; reverse hops stay **open** until
   pure green (do not mark controller_dev for scaffold-only kraid→eye)

## Do not
- Edit STATUS.md continuous claims
- Change controllers or progression edges
- Mark reverse hops continuous or controller_dev without pure green
- Touch `continuous.py`

## Acceptance
- [ ] CSV rows for each reverse edge id above
- [ ] `uv run python super_metroid/scripts/export/kpdr_tracker.py` succeeds
- [ ] No continuous claim for unverified reverse hops
- [ ] Diff summary in final message

## Verify commands
```bash
uv run python super_metroid/scripts/export/kpdr_tracker.py
# quick sanity: reverse edge names appear in CSV
rg 'kraid_to_eye_return|eye_to_baby_return|zeela_to_warehouse_return' \
  super_metroid/docs/routes/KPDR_TRACKER.csv
```
