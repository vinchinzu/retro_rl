# SM-K4-R-01 Residual

- Source: **MISSING**. No `post_kraid_to_eye_return.state` (or equivalent)
  exists under `custom_integrations/SuperMetroid-Snes/scratch/`. The eye-room
  files present are `dev_*` or unrelated reconstructions and are not an
  ordinary post-Varia chained source.
- Change: added the bounded left-facing beam-shot/fuse door-open sequence to
  `play_eye_to_baby_return`, then retained the existing left spin-push.
  This mirrors `play_eye_to_kraid` and the established reverse-door pattern;
  no other reverse hop was changed.
- Pure result: **blocked on source**. The pure probe was not run because using
  a dev door-warp fixture would invent the required chained source.
- Next planner step: produce
  `scratch/post_kraid_to_eye_return.state` through the 06B chain, then run the
  `eye-to-baby-return` pure probe from that named eye-room state.
- Non-claims: no continuous claim, STATUS promotion, graph verification
  promotion, or progression/capacity RAM forge.
