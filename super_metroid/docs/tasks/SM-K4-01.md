# TASK SM-K4-01: Lock K4 reverse-return edge contract in unit tests

## Recipe step
2 graph edge (test lock only — no production graph change)

## Context (minimal)
- Continuous tip verified: power-on → Varia (`--to varia`, 101,954f)
- Next play: K4 return spine after pure `varia_to_kraid` (controller_dev)
- Graph: `START_TO_SPEED_GRAPH` in `progression.py`
- First reverse hop pure green; remaining return edges are `unverified`

## Read first (only these)
- `super_metroid/tests/test_progression.py` — existing `test_k4_graph_varia_to_speed_scaffold`
- `super_metroid/progression.py` — `_K4_SPEED_EDGES` return chain (~1294–1355)
- `super_metroid/docs/TASK_TEMPLATE.md` — acceptance style

## Do
1. Add a focused unit test (new function in `test_progression.py`) that asserts
   the **return edge id chain** from Varia (`0xA6E2`) through Warehouse/Business:
   - `varia_to_kraid`
   - `kraid_to_eye_return`
   - `eye_to_baby_return`
   - `baby_to_kihunter_return`
   - `kihunter_to_zeela_return`
   - `zeela_to_warehouse_return`
   - then reuses existing continuous `warehouse_to_business` (or assert path reaches
     Business `0xA7DE` with that hop present)
2. Assert verification levels:
   - `varia_to_kraid` == `controller_dev`
   - each of the five `*_return` edges == `unverified`
3. Keep the existing `test_k4_graph_varia_to_speed_scaffold` green; extend or
   complement it — do not weaken its path target list to Speed.

## Do not
- Edit `progression.py` production edges unless a clear bug blocks the test
- Touch `continuous.py`, `STATUS.md`, or controllers
- Claim continuous / integrity
- Broad refactors

## Acceptance
- [ ] `uv run pytest super_metroid/tests/test_progression.py -q` green
- [ ] New asserts cover edge ids + verification matrix for return chain
- [ ] No unrelated file churn
- [ ] Diff summary in final message

## Verify commands
```bash
uv run pytest super_metroid/tests/test_progression.py -q
```

## Done when
Files changed + pytest output + note if `path_verification` blocking edge
behavior needs a separate card (do not change it here without failing tests).
