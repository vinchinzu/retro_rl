# Gut before Speed

RTA / button-press work (**Speed**) is not a fail line until a **Skill** can
be A/B’d through a stable load-pin → **Composer** play → compare loop.
**Gut** (merge into the Composer or delete) may run in the same week as
Speed; Speed may not mint a runner or rewrite that loop. We dropped the
old “first credits, then rewrite toward speed” split because the flagship
trees cannot A/B a button sequence without exploding tooling.

**Considered:** keep CONTEXT “rewrite” as a later speed pass and treat
structure as optional hygiene — rejected; agents never prune, and Speed
work was minting probe CLIs. Freeze every current dual CLI as the loop —
rejected; that freezes Super Metroid’s probe zoo.
