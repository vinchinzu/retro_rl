# Harvest is Clean; resource writes are retired

Published tapes and rung evidence use controller input only. No RAM writes.
Resource pokes were allowed as a way to speed early work; that phase is over.
Once the skill functions are Clean, remaining failures are native ROM edges,
not bytes we patch. A clear basic path comes first; then rewrite.
