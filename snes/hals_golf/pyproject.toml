[project]
name = "hals-golf"
version = "0.1.0"
description = "Hal's Hole in One Golf autoplay bot"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["hals_golf"]
